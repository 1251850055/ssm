1.Collection 是最基本的集合接口
	a.List(子接口):(底层是用数组实现的)
		元素有先后次序，元素有index(下标)位置，
		元素可以重复,继承了Collection接口
		也就是说List可以存储一组不唯一有序的对象
	
	使用ArrayList 类动态存储数据
		对数组进行了封装，实现长度可变的数组
		并且添加的数据都将转换成Object类型
		而在数组中只能添加同一数据类型的数据
	使用LInkedList 类动态存储数据 
		LinkedList采用链表存储方式，优点在于插入，删除
		元素时效率比较高，但是LinkedList	 查找效率
		比较低
			
			
	
	
	
	b.Set 元素没有先后顺序(没有下标),不能重复添加,
		继承了Collection接口
		__________________________________
			List集合可以存储一组不唯一，有序的对象
			Set集合可以存储一组唯一，无序的对象
		-----------------------------------------------------------	
	HashSet 实现了Set接口,是Set结合常用的一个实现类
	HashSet 的特点:
		1) 集合内的元素是无序排列的
		2) HashSet 是非线程安全的
		3) 允许集合元素为null	
	TreeSet:
		它是Set的一个变种，它比较特殊，可以实现
		排序功能.当有元素要添加到集合中时，
		会自动按照某种规则插入到有序的对象序列中
		
	Map:
		描述了(Key:Value)成对放置的集合
		接口存储一组成对的 键-值 对象，提供Key到Value的
		映射，通过Key来检索
			map接口中Key不要求有序，不允许重复
			value同样不要求有序，但允许重复	
			
 Collections
 	同数组工具类Arrays,集合的工具类
 	提供了很多方法,诸如排序，查找.......
 	
 	
 	Irerrator   hastnext()判断是否有下一个值     和next()   是把值给拿出来      
 		
 要排对象的序，必须这个对象要实现Comparabel 这个接口
 重写compareTo 这个方法		
 		
		